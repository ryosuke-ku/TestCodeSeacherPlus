// clone pairs:8901:76%
// 9867:flink/flink-core/src/main/java/org/apache/flink/api/common/operators/util/FieldList.java

public class Nicad_t2_flink327
{
	public boolean isValidUnorderedPrefix(FieldSet set) {
		if (set.size() > size()) {
			return false;
		}
		
		List<Integer> list = get();
		for (int i = 0; i < set.size(); i++) {
			if (!set.contains(list.get(i))) {
				return false;
			}
		}
		return true;
	}
}